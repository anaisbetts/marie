import * as React from 'react';
import { Scaffold } from '../components/scaffold';

const SettingsPage: React.FC = () => {
  return <Scaffold buttonIndex={1} title="Settings" />;
};

export default SettingsPage;
